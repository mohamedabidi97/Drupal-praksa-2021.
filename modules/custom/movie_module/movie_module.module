<?php

/**
 * Implementing hook_theme().
 */
function movie_module_theme($existing, $type, $theme, $path) {
  return array(
    'movie_list' => array(
      'variables' => array('items' => array(), 'title' => '')
    )
  );
}
